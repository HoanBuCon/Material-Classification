Đây là dự án nâng cấp lại dự án cũ của một người nào đó đã làm dự án này trước đó nhưng vì dự án đã làm trước đó chưa thực sự tối ưu nên tôi tạo dự án này để nâng cấp dự án đó 💖

copy 2 file `cbam.py` và `simam.py` tại folder root/ultralytics/nn/modules vào trong môi trường ảo/Lib/site-packages/ultralytics/nn/modules để mô hình nhận kiến trúc attention
(Lười thì copy dòng trên ném cho AI viết script cho dễ :v)